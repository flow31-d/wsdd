#!/usr/bin/env bash
# WSD pre-commit hook — validates WSD contract before each commit.
# Auto-installed by wsd-method install. Versioned copy: scripts/git-hooks/pre-commit
# Reinstall: ./.wsd/bin/wsd hooks

set -euo pipefail

if [[ -f "scripts/wsd_check.sh" ]]; then
  bash scripts/wsd_check.sh --risk L0 . || exit 1
fi
